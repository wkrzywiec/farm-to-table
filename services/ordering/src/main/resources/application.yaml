spring:
  application:
    name: 'food-delivery_ordering'

management:
  endpoints:
    web:
      exposure:
        include: '*'

---
server:
  port: 8082

spring:
  config:
    activate:
      on-profile: dev

---
spring:
  config:
    activate:
      on-profile: prod